import { normalize } from "../utils/responsive"

export const spacing = {
  xs: normalize(4),
  sm: normalize(8),
  md: normalize(12),
  lg: normalize(16),
  xl: normalize(20),
  xxl: normalize(24),
  xxxl: normalize(32),
  huge: normalize(40),
  massive: normalize(50),
}
